# user.md

> 这是 AI 读完你所有文章后，提炼出的「稳定的你」——身份、性格、价值观、偏好。不是一次性的总结，是会随着你的笔记增长而持续演化的画像。

## 身份

一个**写了十年代码的全栈工程师**。但「全栈」这个词太泛了——更准确地说，你是那种**不满足于会用，必须知道为什么**的人。每一个工具背后你都要搞清楚它的设计决策，每一段源码你都要拆开来看它怎么运转。从 Docker 的 namespace 到 Python 的 metaclass，从 K8s 的声明式 API 到 Rust 的所有权模型——你对「表面使用」有生理性的排斥。

同时用 macOS 和 Linux，CLI 重度依赖者。GUI 能省则省，命令行里的每一毫秒延迟都值得被优化。

## 性格

**务实，反鸡汤**。你写在 README 第一段的话就是最好的概括——「这里没有正确的废话，只有踩过的坑、读过的源码、用过的工具——每一个结论背后都有一段不想回忆的 debug 经历」。你不写那种「XX 入门指南」式的罗列文章，你写的是**踩坑之后的理解**。

**系统化**。你不会就一个主题写单篇文章然后跳到下一个。你要建文件夹、建系列、建索引——Docker → Compose → K8s → Helm，四篇串成一条完整的演进线。这是工程思维在知识管理上的投射：散装的知识不可维护。

**带点自嘲**。「写了十年代码，再不输出点东西脑子要溢出了」——这种幽默感贯穿始终。你不把自己当老师，更像是一个记笔记给自己看的人，只是恰好公开了。

**深度优先，不赶时髦**。你对新工具的态度是审视的——先搞清楚它解决了什么真问题，再决定值不值得用。Compound Engineering 那篇文章最能说明这一点：你关注的不是一个工具好不好用，而是**它是否让下一次工作更简单**。

## 价值观

你反复在文章里透露出这几条：

1. **产品要从自用长出来**。你写的工具都是自己会用的——LOF 溢价监控是你自己要看套利机会，知识库是你自己要用 AI 检索。不是「我觉得别人需要」，是「我需要」。

2. **工程复利**。不是做完就完了，每次提交都应该为下一次铺路。这也是为什么你的文章系列之间有明确的递进关系——前一篇的痛点，是后一篇存在的原因。

3. **可审计、可追溯**。从 LOF 系统的每个数据字段标注代码位置，到源码阅读文章精确到路由文件和行号——你相信**没有来源的结论不可信**。

4. **KISS / DRY / YAGNI**。你把这些软件工程原则写进了全局 CLAUDE.md，说明这不是说说而已——你在每一次决策中都在用。

5. **追求的不是爆发，是经常性**。从你选择的工具栈（纯 Markdown、零构建、零依赖）到你的写作节奏（每天一篇、持续积累），都在表达同一个偏好：**可持续的系统，胜过一次性的辉煌**。

## 技术偏好

### 语言

| 语言 | 程度 | 证据 |
|------|------|------|
| Python | 最深入 | OOP 系列 5 篇、装饰器、闭包、模块系统、类型提示、with/yield/async |
| Go | 熟练 | Channel 系列 4 篇、Goroutine 系列 4 篇、单元测试 |
| Rust | 学习中 | 6 篇文章从所有权到生命周期，扎实的系统性学习 |
| Java | 了解 | 仅单元测试一篇 |
| Node.js | 了解 | 事件循环 + 实用特性 |

### 工具栈

- **编辑器/IDE**: Claude Code + VS Code/Cursor
- **容器**: Docker → Docker Compose → K8s → Helm（完整演进路线）
- **macOS 容器运行时**: Colima / OrbStack（拒绝 Docker Desktop 的臃肿）
- **包管理**: uv（Python）、Homebrew
- **版本控制**: Git + GitHub
- **AI 工具链**: Claude Code 为核心，外挂 RTK、CodeGraph、Spec-Kit、gstack、Understand-Anything
- **知识管理**: 纯 Markdown + Git，零构建零依赖
- **备份**: 坚果云 WebDAV
- **通知**: 飞书 Webhook

### 领域知识

- **量化交易/金融数据**: LOF 基金溢价监控系统是你自己设计的，从数据源到告警全链路。两套源码阅读（Sequoia-X 和 QuantDinger）都选了量化方向。
- **AI Agent 工具链**: 知识库里最大的一个分类（13 篇），从 RTK 到 Agent Reach，从 Spec-Kit 到 Compound Engineering——你在系统性测绘 AI 编程工具的边界。
- **基础设施**: Docker、K8s、Helm、systemd/launchd、内网穿透——你不只是 Dev，你是自己搭过全套生产环境的人。

## 写作风格

- **中文 + 英文专业术语**。namespace、metaclass、decorator 不做翻译——你知道这些词在中文里没有精确对应，强行翻译反而增加理解成本。
- **说 WHY 不说 WHAT**。你的文章从来不是 API 文档翻译——每一段都在解释「为什么这样设计」「不这样会有什么问题」。
- **实战场景控制在 1-2 个**。你不会为了显得丰富而堆砌案例。一个完整的、有真实终端输出的例子，比十个伪代码片段有用。
- **Mermaid 做图**。flowchart 表达数据流/流程，mindmap 表达层次分类。
- **系列优先于单篇**。复杂主题拆成 4-5 篇，每篇可独立阅读，但串起来是一条完整的认知曲线。

## 你反复出现的模式

1. **从痛点出发**。每一篇文章的开头都是一段「你有没有遇到过这个问题」。你的写作结构是：痛点 → 原因 → 解决方案 → 为什么这样解决。

2. **对抗复杂性**。CodeGraph 是为了对抗 grep 全仓，RTK 是为了对抗 token 浪费，Compound Engineering 是为了对抗代码腐化——你的技术选择有一条清晰的主线：**让复杂的东西变简单**。

3. **薄封装优于厚抽象**。你对 QuantDinger MCP Server 的评价是「薄封装策略」，对 Colima 的喜爱是「只做一层薄薄的 VM 抽象」——你偏好那些让你离底层更近而不是更远的工具。

4. **自托管优先**。GitLab 自托管、LOF 本地部署、QuantDinger 本地部署——你宁可多花时间配置，也不愿意把数据和控制权交给云服务。
